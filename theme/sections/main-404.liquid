<section class="section container error-page">
  <h1>{{ 'templates.404.title' | t }}</h1>
  <p class="text-muted">{{ 'templates.404.subtext' | t }}</p>
  <a href="{{ routes.root_url }}" class="btn btn-primary">{{ 'templates.404.link' | t }}</a>
</section>

{% schema %}
{
  "name": "t:sections.main-404.name",
  "settings": [

  ]
}
{% endschema %}
